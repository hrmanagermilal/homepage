# Copy this to .env and fill in values for your environment.
# Leave APP_BASE_URL blank on localhost — auto-detected from request headers.
# Set it on EC2 (or any host behind a proxy that strips port numbers).

APP_BASE_URL=
